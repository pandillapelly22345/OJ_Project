i#include <iostream> // Required for input/output operations

int main() {
    int num1, num2, sum;
    std::cin >> num1;
    std::cin >> num2;
    sum = num1 + num2;

    // Display the result to the user
    std::cout<< sum << std::endl;

    return 0; // Indicate successful program execution
}